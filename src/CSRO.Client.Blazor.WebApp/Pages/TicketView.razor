@page "/ticketview/{TicketId}"
@using CSRO.Client.Services.Models

<TicketCsro OperationTypeTicket="@OperatioType.View" TicketId="@TicketId">

</TicketCsro>

@code {
    [Parameter]
    public string TicketId { get; set; }
}
